/// <reference types="node" />
import { Transform } from "stream";
import { Options } from "./options";
declare const _default: ({ template, type, keepCase }?: Options) => Transform;
export = _default;
